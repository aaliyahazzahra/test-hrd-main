# .env.example - Example Environment Variables

# --- PostgreSQL Database Configuration ---
POSTGRES_USER=appuser
POSTGRES_PASSWORD=your_strong_password
POSTGRES_DB=employees_db
POSTGRES_PORT=5432
DB_HOST=db

# --- Application Configuration (FastAPI) ---
APP_SECRET_KEY=a_very_secret_key_for_hashing_or_auth
APP_HOST=0.0.0.0
APP_PORT=8000

# --- Frontend Configuration (React) ---
# NOTE: Frontend variables are usually prefixed with REACT_APP_
# They might not be needed in the docker-compose setup, but good practice.
# REACT_APP_API_BASE_URL=http://localhost:8000/api